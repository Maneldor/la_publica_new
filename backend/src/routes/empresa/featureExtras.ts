import { Router } from 'express';

const router = Router();

// Temporalmente deshabilitado para evitar errores de compilación
// TODO: Implementar rutas de extras cuando se resuelvan los tipos

export default router;